function onStepHit()
{
	switch (curStep)
	{
		case 703:
			PlayState.changeDadIcon(true);
		case 831:
			PlayState.changeDadIcon(false);
		case 895:
			PlayState.changeDadIcon(true);
		case 959:
			PlayState.changeDadIcon(false);
	}
}
